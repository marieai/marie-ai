Extract the **Check Amount** , **Check Number** , **PIN**, **TIN**, from the header section at the top of the document using a key-value format.
Ensure that only one complete set of these header key values is identified and extracted.

### Annotation Guidelines:
-  Maintain strict **document order**—do not reorder or omit necessary data.
- **Follow the designated annotation format strictly.**
- **Extract only the most relevant row per claim.**
- **Ensure clarity, accuracy, and consistency** in all annotations.