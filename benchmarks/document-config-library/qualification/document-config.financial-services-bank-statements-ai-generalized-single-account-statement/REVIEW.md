# Expected-output review

Status: pending human owner review

`expected-output.draft.json` is an automated transcription of the bundled
three-page sample statement. It is intentionally not named
`expected-output.json`, so the qualification preflight cannot treat it as
approved gold data.

Review checklist:

- Confirm the statement dates, customer identity, address, and balances.
- Confirm all 20 non-check transactions and their classifications.
- Confirm all 9 cleared checks, including the source marker on check 1003.
- Confirm that checks are excluded from `transactions`.
- Rename the draft to `expected-output.json` only after recording the reviewer
  name and review date below.

Reviewer: pending

Review date: pending
