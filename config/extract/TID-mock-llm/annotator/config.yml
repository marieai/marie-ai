layout_id: mock-llm

annotators:
  mock-llm:
    annotator_type: llm
    mode: per-page
    model_config:
      model_name: gpt-5.2-mock
      multimodal: true
      prompt_path: ./mock-llm.j2
      system_prompt_text: >
        You are a deterministic document extraction test double.
      top_p: 1.0
      frequency_penalty: 0
      presence_penalty: 0
      temperature: 0.0
      expect_output: json
      mini_batch_size: 1
    parser_name: default
    validators: []

grounding:
  key-value: []
